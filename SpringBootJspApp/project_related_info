# SpringbootJSP

#to connect H2 DataBase ----- we need to create file with like this "demo.mv.db" in the database folder

#DataBase url : http://localhost:8080/SpringBootJspApp/h2-console

#swagger url : http://localhost:8080/SpringBootJspApp/swagger-ui/index.html

#project url :  http://localhost:8080/SpringBootJspApp/"endpoint"

#				http://localhost:8080/SpringBootJspApp/public/health
#				http://localhost:8080/SpringBootJspApp/test
#				http://localhost:8080/SpringBootJspApp/hello


#JobScheduler timimg
# JOBSCHEDULER=${JOB_SCHEDULER:0 30 1 ? * MON-FRI}  --- run at 1:30 AM/PM on Monday to Friday
# JOBSCHEDULER=${JOB_SCHEDULER:0 30 1 ? * MON}  --- run at 1:30 AM/PM on every Monday
# @Scheduled(cron = "${JOBSCHEDULER}",zone = "Asia/Kolkata") --- zone is used for EST time